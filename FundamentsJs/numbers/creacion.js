// ** CREACION DE NUMEROS **//

const number = 8;
const notNumber =  "8";

console.log(`Este es un numero: ${number}. Se pueden realizar operaciones con el`);
console.log(`Ese es un string con un numero: ${notNumber}. No se pueden realizar operaciones con el`);

// Todos los numeros se crean de la misma manera, a sea con let const o var
// Pueden ser enteros, deciamles, negativos

// * Manera poco usual de declarar numeros 
let objectNumber = new Number(80); // Declarar los numeros de esta manera se tiene lo que se conoce como un objeto con un numero 80
console.log(objectNumber);